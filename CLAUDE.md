# マルチエージェントコース

## プロジェクト概要

Claude Codeのマルチエージェント機能を学習する実習プロジェクト

## 技術スタック

- Python 3.14
- パッケージ管理: uv (pipは使用しない)
- テスト: pytest
- フレームワーク: FastAPI

## 開発ルール

- コメントとドキュメントは日本語で記述する
- パッケージの追加は`uv add`を使う
- スクリプト実行は`uv run python`を使う
- テスト実行は`uv run pytest`を使う
- pip install は絶対に使わないこと

## ディレクトリ構成

- src/ : メインのソースコード
- tests/ : テストコード
- docs/ : ドキュメント

## 「おつかれ」コマンド

ユーザーが おつかれ と入力したら、現在のGitリポジトリで以下を自動実行する。

git status と git diff で変更内容を確認

変更内容を簡潔に要約

git add .

変更内容に合った日本語の簡潔なコミットメッセージを作成

git commit -m "<コミットメッセージ>"

git push

コミットメッセージとpush結果をユーザーに報告

変更がない場合はcommitしない。

.env、APIキー、秘密鍵、パスワードなどの機密情報が含まれている可能性がある場合は、コミットせずユーザーへ確認する。

おつかれ の場合は、通常の変更については実行前確認をせず、そのままadd・commit・pushまで行う。

## 「おはよう」コマンド

ユーザーが `おはよう` と入力したら、現在のGitリポジトリで以下を順番に自動実行する。

1. 現在のブランチを確認する。
2. リモートの最新変更を取得する。

   ```bash
   git pull
   ```

3. `git pull` が成功したら、uv環境を同期する。

   ```bash
   uv sync
   ```

4. 完了後、以下を簡潔に報告する。
   - `git pull` の結果
   - `uv sync` の結果
   - 更新されたファイルや依存関係があれば簡単に説明する

### 注意事項

- ユーザーが `おはよう` と入力した場合は、原則として確認を求めず実行する。
- `git pull` でコンフリクトが発生した場合は、`uv sync` を実行せず処理を停止し、競合しているファイルを報告する。
- ローカルに未コミットの変更があり、`git pull` によって問題が起きる可能性がある場合は、勝手に変更を破棄・stash・commitしない。
- `uv sync` でエラーが発生した場合は、エラー内容と原因候補を簡潔に説明する。
